<%= form_for(@district_list) do |f| %>
  <% if @district_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@district_list.errors.count, "error") %> prohibited this district_list from being saved:</h2>

      <ul>
      <% @district_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
